# Core
ultralytics>=8.0.0          # YOLOv8 face detection
insightface>=0.7.3          # ArcFace / buffalo_l embeddings
onnxruntime>=1.16.0         # CPU inference backend for InsightFace
# onnxruntime-gpu>=1.16.0     # GPU inference (install instead of cpu if CUDA available)

# Vision
opencv-python>=4.8.0
numpy>=1.24.0

# Optional: GPU-accelerated tracking backends
# lap>=0.4.0                # required for ByteTrack/BotSORT inside ultralytics
# deep-sort-realtime>=1.3.2 # DeepSORT tracker (alternative)

# Utilities
Pillow>=9.0.0
